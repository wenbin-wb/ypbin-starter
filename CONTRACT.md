# ypbin-starter 对外契约规范

> 本文件是「单体后端」与「微服务后端」共同遵守的对外合同。只要两套后端都遵守本规范，
> 前端就无需关心后端是单体还是微服务，可复用同一套调用逻辑。

## 0. 适用范围

- 单体应用：直接引入 L1 基础层能力模块。
- 微服务应用：在 L1 基础上叠加 L3 `cloud-*` 模块，由网关统一入口。
- 两者对前端暴露的响应结构、错误码、分页、鉴权头、序列化规则必须完全一致。

## 1. 统一响应体

所有接口统一返回 `R` 结构（`cn.ypbin.starter.core.model.R`）：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {},
  "success": true,
  "timestamp": "2026-07-31T18:00:00"
}
```

字段约定：

| 字段 | 类型 | 说明 |
|---|---|---|
| `code` | int | 业务状态码，见第 3 节 |
| `message` | string | 提示信息，可直接展示给用户 |
| `data` | any | 数据载荷，失败时为 `null` |
| `success` | boolean | 是否成功，等价于 `code == 200` |
| `timestamp` | string | 服务端响应时间 |

前端判定成功：优先判 `success`，或判 `code === 200`。

## 2. HTTP 状态码约定

- 业务响应一律返回 **HTTP 200**，由 `code` 区分结果，前端不需要同时处理 HTTP 状态码和业务码两套逻辑。
- 网关异常、下游异常也会被转成 HTTP 200 + `R` 结构（微服务侧由 `cloud-gateway` 统一处理）。
- 真正的传输层错误（网络中断、502/504 等）才由 HTTP 层暴露，前端按网络异常处理。

## 3. 错误码规范（HTTP 语义分段）

框架内置错误码沿用类 HTTP 语义的三位码（`GlobalErrorCode`）：

| code | 含义 | 前端典型处理 |
|---|---|---|
| 200 | 成功 | 正常渲染 |
| 400 | 请求参数有误 | 提示校验信息 |
| 401 | 未认证 / 登录过期 | 跳转登录 |
| 403 | 无访问权限 | 提示无权限 |
| 404 | 资源不存在 | 提示不存在 |
| 409 | 业务处理失败 | 展示 `message` |
| 429 | 请求过于频繁（限流/熔断） | 提示稍后重试 |
| 500 | 系统内部错误 | 提示系统繁忙 |

业务自定义错误码约定：

- **不占用上述三位语义码**，业务码使用四位及以上（如 `1001`、`20001`），由各业务系统维护自己的枚举实现 `ErrorCode`。
- 前端对未知业务码统一降级为「展示 `message`」，不需要穷举。
- 建议业务码按系统内模块再分区段（如订单 `10xxx`、用户 `11xxx`），由业务系统自行约定并记录。

## 4. 分页约定

请求参数（`PageQuery`，可作为查询条件父类）：

| 参数 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `page` | long | 1 | 页码，从 1 开始 |
| `pageSize` | long | 10 | 每页条数 |
| `sortField` | string | — | 排序字段（可选） |
| `asc` | boolean | true | 是否升序 |

返回结构（`PageResult`，包在 `R.data` 中）：

```json
{
  "code": 200,
  "success": true,
  "data": {
    "items": [],
    "total": 100,
    "page": 1,
    "pageSize": 10,
    "pages": 10
  }
}
```

`pages` 为总页数，由 `total` 与 `pageSize` 推导。

## 5. 序列化约定

- **Long / BigInteger 等大数一律序列化为字符串**，避免前端 JS 大数精度丢失。前端应把 id 类字段当字符串处理。
- 时间统一格式，由 `ypbin-starter-json` 的 Jackson 配置控制，单体与微服务一致。
- 脱敏字段通过 `@Sensitive` 控制，序列化后已脱敏，前端直接展示。

## 6. 鉴权与请求头约定

前端只做一件事：**登录后携带 token**（默认 `Authorization` 头，具体以 sa-token 配置为准）。后端两套内部处理不同，但对前端透明：

| 环节 | 单体 | 微服务 |
|---|---|---|
| token 校验 | 应用内 sa-token 直接校验 | 网关 `cloud-gateway` 统一校验 |
| 未登录/过期 | 返回 `code=401` | 网关返回 `code=401`（HTTP 200） |
| 无权限 | 返回 `code=403` | 网关或下游返回 `code=403` |
| 用户身份 | 应用内直接获取 | 网关清洗外部头后签发内部身份头，`cloud-core` Feign 透传 |

内部身份头（**由网关签发，前端不得传、传了会被网关清洗掉**）：

| 头名 | 含义 |
|---|---|
| `X-Request-Id` | 请求链路 ID，入口生成或沿用，贯穿调用链 |
| `X-User-Id` | 当前登录用户 ID |
| `X-User-Name` | 当前登录用户名 |
| `X-Tenant-Id` | 租户 ID |
| `X-Dept-Id` | 部门 ID |
| `X-Roles` | 角色标识 |

安全约定：这些身份头**只在服务内部可信链路中流转**。网关默认清洗客户端传入的同名头，防止身份伪造；单体应用不依赖这些头，直接从登录上下文取值。

## 7. 前端对接要点小结

1. 只认 `R` 结构，判 `success` / `code`。
2. 业务失败展示 `message`，`401` 跳登录，`403` 提示无权限，`429` 提示稍后重试。
3. id 类字段按字符串处理。
4. 分页用 `page/pageSize`，读 `items/total/pages`。
5. 只带 token，不要自己造 `X-User-Id` 等内部头。

只要单体和微服务两套后端都以本规范为准，前端代码可原样复用，无需按后端架构分叉。

# ypbin-starter 本地微服务自测环境变量模板
#
# 用法：复制为同目录 .env 后按需修改（.env 已被 .gitignore 忽略，切勿提交真实口令）。
#   cp deploy/.env.example deploy/.env

# Sentinel Dashboard 控制台账号密码
SENTINEL_AUTH_USERNAME=sentinel
SENTINEL_AUTH_PASSWORD=change-me

# MySQL root 口令
MYSQL_ROOT_PASSWORD=change-me
